# PrivaHub PWA

PrivaHub 移动端 PWA 版本，基于 React + Vite + TypeScript 构建。

## 本地开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

构建产物在 `dist/` 目录。

## 部署

本项目部署在腾讯云 EdgeOne Pages，通过 Gitee 仓库关联自动构建。

- 构建命令：`npm run build`
- 输出目录：`dist`
